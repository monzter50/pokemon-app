const initinialState = {
  pokemons: [],
  loading: false,
  error: false,
};
const findPokemon = (state = initinialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default findPokemon;
